# SurplusAI 用户 OAuth 账号池下一步实施计划

## 目标

基于当前 `sub2api` / SurplusAI fork，继续二次开发为内部团队可用的算力回收平台。平台只支持用户贡献 OAuth 账号，不支持用户上传 API Key。用户可以把自己的 OAuth 账号加入公共账号池，平台统一调度给其他内部用户使用，并记录账号被使用的额度。

## 当前已确认状态

- 项目镜像 `surplusai:local` 已能构建并启动。
- 管理员初始化、用户登录、注册关闭、普通用户禁止访问管理员接口等基础行为已验证。
- 管理员侧已验证 API Key 类型账号创建会被拒绝，OAuth 类型账号可以创建。
- 当前账号管理仍主要是管理员能力，普通用户还不能贡献和管理自己的 OAuth 账号。
- 当前账号模型没有账号归属用户字段，因此无法区分某个 OAuth 账号属于哪个贡献者。
- 当前已有 5h 窗口限额和周额度字段基础，但调度逻辑主要覆盖原项目账号类型，不足以支持统一的用户贡献账号策略。

## 需要补齐的产品行为

1. 用户可以上传或授权自己的 OAuth 账号给平台使用。
2. 用户可以设置自己账号的 5h 使用限额。
3. 用户可以设置“当周剩余额低于指定阈值时不参与调度”。
4. 用户端可以看到全部账号池的只读概览。
5. 用户只能管理自己贡献账号的调度开关和限额配置。
6. 用户不能查看、修改、关闭其他用户贡献账号的调度状态。
7. 管理员保留全局管理和审计能力。

## 后端实施计划

### 1. 数据模型

- 在 `accounts` 表增加 `owner_user_id`，可为空。
- `owner_user_id = NULL` 表示系统或管理员维护账号。
- `owner_user_id = 当前用户 ID` 表示用户贡献账号。
- 更新 Ent schema、迁移、Service `Account` 模型、Repository 映射和创建/更新逻辑。
- 对用户贡献账号强制 `type = oauth`，拒绝 API Key、Bedrock 或其他非 OAuth 类型。

### 2. 用户侧账号池 API

新增普通用户接口，和管理员账号接口分离：

- `GET /api/v1/accounts/pool`
  - 返回全账号池安全列表。
  - 不返回 credentials、完整 extra、proxy、refresh token、内部错误堆栈。
  - 返回 `is_mine` 标识，用于前端判断是否可编辑。

- `POST /api/v1/accounts/oauth`
  - 当前登录用户上传或授权 OAuth 账号。
  - 自动写入 `owner_user_id`。
  - 强制校验 OAuth-only。

- `PATCH /api/v1/accounts/:id/schedulable`
  - 只有账号 owner 可以修改自己的调度开关。
  - 管理员仍通过管理员接口管理全局调度。

- `PATCH /api/v1/accounts/:id/limits`
  - 只有账号 owner 可以修改自己的 5h 限额和周剩余额阈值。
  - 校验数值范围，禁止负数和异常大值。

### 3. 安全 DTO

新增用户端账号池 DTO，例如 `UserAccountPoolItem`：

- 可返回：账号 ID、名称、平台、类型、状态、是否本人账号、调度开关、5h 限额、5h 当前消耗、周额度上限、周已用、周剩余额阈值。
- 不返回：OAuth credentials、refresh token、access token、完整 extra、代理配置、管理员备注、内部错误详情。

### 4. 调度策略

统一实现用户贡献账号调度策略，避免只在单一路径里判断：

- 账号必须是 OAuth 类型。
- 账号必须处于可调度状态。
- 如果设置了 5h 限额，则当前 5h 使用量必须小于该限额。
- 如果设置了周剩余额阈值，则 `周额度上限 - 周已用` 必须大于等于该阈值。
- 账号被 owner 关闭调度后，不能被任何网关路径选中。

需要接入的调度路径至少包括：

- Anthropic / Claude 网关。
- OpenAI 兼容网关。
- Gemini messages compat 网关。
- WebSocket 或其他复用账号调度器的路径。

### 5. 额度记录

- 确认所有成功请求都会把用量累积到对应账号。
- 将账号级用量统计扩展到用户贡献 OAuth 账号。
- 5h 窗口用量和周用量应保持一致的单位定义，避免一个使用 cost、另一个使用 token 或 quota。
- 对无 usage 返回的请求，保留现有估算或兜底逻辑，并明确是否计入贡献账号额度。

### 6. 权限边界

- 普通用户可以看到账号池概览，但不能获取敏感字段。
- 普通用户只能修改 `owner_user_id = 自己 ID` 的账号。
- 普通用户不能调用管理员账号创建、编辑、删除、批量更新接口。
- 管理员可以查看账号 owner，并能禁用、编辑或删除违规账号。
- 管理员操作要覆盖用户设置，例如管理员禁用后，即使 owner 打开调度也不能被使用。

## 前端实施计划

### 1. 新增用户侧账号池页面

- 在普通用户导航中增加“账号池”入口。
- 页面展示所有账号池账号。
- 对本人账号显示可编辑控件。
- 对其他用户账号只显示只读状态。

### 2. 上传 OAuth 账号流程

- 提供 OAuth 账号添加入口。
- 复用当前项目已有 OAuth 创建逻辑时，需要隐藏或禁止 API Key 类型选项。
- 添加成功后刷新账号池列表。

### 3. 本人账号管理控件

- 调度开关：只对 `is_mine = true` 的账号显示可操作开关。
- 5h 限额：只允许 owner 编辑。
- 周剩余额阈值：只允许 owner 编辑。
- 非 owner 账号不展示可点击开关，避免前端误导。

## 测试计划

### 后端单元测试

- 普通用户创建 OAuth 账号成功。
- 普通用户创建 API Key 账号失败。
- 普通用户只能修改自己的账号调度开关。
- 普通用户修改他人账号调度开关返回 `403`。
- 普通用户只能修改自己的限额配置。
- 用户账号池列表不泄露 credentials、token、完整 extra。
- 5h 限额达到后账号不再被调度。
- 周剩余额低于阈值后账号不再被调度。
- 管理员禁用账号后，owner 不能通过用户接口绕过。

### 集成测试

- 启动 Docker 测试栈。
- 管理员创建成员用户。
- 成员用户上传 OAuth 账号。
- 另一个成员用户能在账号池看到该账号，但不能管理。
- 使用网关请求产生用量后，账号用量统计更新。
- 达到 5h 或周阈值后，调度器自动跳过该账号。

### 前端验证

- 普通用户账号池页面可打开。
- 本人账号显示管理控件。
- 他人账号只读。
- 限额编辑、调度开关保存后状态正确刷新。
- 浏览器网络面板确认前端没有拿到敏感字段。

## 上线前检查清单

- Docker 镜像重新构建成功。
- 后端测试通过。
- 前端 typecheck 和 build 通过。
- 使用本地 Docker 栈完成管理员和普通用户行为验收。
- 确认生产环境关闭公开注册。
- 确认默认管理员密码已通过环境变量覆盖。
- 确认 OAuth token 加密、日志脱敏和错误响应脱敏。
- 确认迁移脚本可在已有数据库上安全执行。

## 建议执行顺序

1. 先补 `owner_user_id` 数据模型和迁移。
2. 再补用户侧账号池 API 与安全 DTO。
3. 接入用户贡献账号的权限校验。
4. 统一调度策略，把 5h 限额和周剩余额阈值接入所有网关路径。
5. 完成用量累积逻辑。
6. 新增前端账号池页面。
7. 补测试并重新构建 Docker 镜像。
8. 用本地 Docker 栈跑一轮上线前验收。

